{
  "name": "railway-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:scraper": "cd booking-scraper-service && npm install",
    "build": "cd booking-scraper-service && npm run build",
    "start": "cd booking-scraper-service && npm start"
  }
}
